version: 0.2
phases:
  install:
    runtime-versions:
      php: 7.4
  pre_build:
    commands:
      - echo ""
      
  build:
    commands:
      - echo "Running tests"
      
  post_build:
    commands:
      - echo "Running additional post-build steps"
      

artifacts:
  files:
    - '**/*'
  base-directory: '/var/www/html/Recruitment_Management_System/'
